# Experimental Notice

**nomue Protocol Release 1 Public Draft verifier package.**

- This repository is aligned to the published nomue Protocol Release 1 Public Draft.
- The Release 1 support target remains `EXPERIMENTAL`; it is not a Stable 1.0 compatibility promise.
- The verifier package itself remains experimental and currently retains package version `0.2.1-rc.0`.
- The exact Release 1 bundle `urn:nomue:bundle:itgc-guarantee:0.2.1-draft.1` is marked `public_release: true` in the published Protocol registry.
- Breaking changes remain possible only through successor versions under the Protocol versioning rules; the published Release 1 snapshot itself is immutable.
- Exact Protocol content, signed-source, decision, tag, and snapshot identities are recorded in `SOURCE-PIN.json`.
