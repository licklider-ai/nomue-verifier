# Frozen development material

Everything under `development/` is frozen as of 2026-09-26.

- It is unissued development material. It adds no supported check, is outside
  the npm package, and is not run by CI.
- Keep the files as they are. Do not extend them or build new work on them here.
- This repository implements only checks that are public in a nomue Protocol
  release or public release candidate (see [AGENTS.md](../AGENTS.md)). If the
  Protocol publishes a check that covers this area, implement it from that public
  specification. These files are not a source of semantics.
