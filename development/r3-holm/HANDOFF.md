# D1 component checkpoint and continuation

Historical initial checkpoint at `a829970`: independent review returned
GO_FOR_D1_CONTINUATION, with two MINOR repairs carried into the next unit.
The current scope and review request are in [LOCAL-CHECKS-HANDOFF.md](LOCAL-CHECKS-HANDOFF.md).
The original coverage table below describes only that first component checkpoint;
**full D1 and all 44 full-call acceptance cases remain open**. No new public
report/refusal schema, supported bundle, release identifier or source pin is issued.

## Acceptance-case accounting

All numbers refer to the Protocol R3D locators. Component evidence is only a
partial check of that case's prerequisites, never a full-call PASS.

| R3D cases | Evidence here                                                                                           | Still required                                                                                    |
| --------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 01-04, 06 | Graph tests for local/context separation and gating, including 192 combinations                         | Actual schema, D0, expected-context and numerical evaluations with scoped reports                 |
| 05        | Stored reference ignores declared digest; graph handles I failure                                       | Actual declared-digest comparison and full report reference binding                               |
| 07-10     | S-failure graph and non-object/byte handling only                                                       | Faithful reference admission, schema failure and exact bundle routing                             |
| 11-13     | Existing strict parser exercised for syntax/UTF-8/duplicate/Unicode                                     | Invocation-selected refusal output and precedence                                                 |
| 14-15     | Exact whitespace/newline P(B) vectors and K-to-I/A graph                                                | Full K-failure report, copied original bytes, no-forward lifecycle                                |
| 16-20     | C-error/mismatch graph only                                                                             | Separate expected input acquisition, validation, identities and no-read gating                    |
| 21-23     | H/A dependency logic only                                                                               | Actual domain checks, unchanged numerical kernel and fixed numerical fixtures                     |
| 24-25     | Raw/parsed bounds and injected early/late budget exceptions                                             | Shared real budget, refusal projection and no partial result escape                               |
| 26-27     | None                                                                                                    | Controlled host, cgroup/deadline/cancellation/setup/cleanup evidence                              |
| 28-31     | Exact internal row validation; 15 mutations reject false passes, missing blockers and unrelated reasons | Public schema, section/scope validation and full-call output controls                             |
| 32-34     | None                                                                                                    | Versioned report/refusal and legacy/multi-bundle dispatcher                                       |
| 35-36     | No forwarding API; no synthesized context                                                               | Original-byte full-call forwarding, trusted completion and invalid inner-output rejection         |
| 37        | Separate schema-blocking and budget-interruption checks only                                            | Combined full-call refusal precedence                                                             |
| 38-39, 42 | K/D/C crossed outcomes and transitive/multiple reason union                                             | Connect actual evaluations and versioned output                                                   |
| 40-41     | None                                                                                                    | Record/revision context comparison and legacy extra-argument refusal                              |
| 43        | Non-object/nonfinite/projection-capable byte handling and interrupt tests                               | Full-call canonicalization/projection/digest-failure refusal                                      |
| 44        | Stored digest differs from reserialized whitespace/newline targets; canonical P(B) cross-check          | Fault injection of the integrated canonicalizer/extractor and proof that I/forwarding cannot pass |

## Author adversarial review

- Storage risk: reserialization can erase the evidence K is intended to check.
  The implementation retains original bytes, slices byte offsets, and uses
  literal expected projections across first/middle/last/absent integrity, escaped
  keys, nested lookalikes, quotes, non-ASCII and trailing-newline inputs.
- Graph risk: a pass may suppress or overwrite another scoped failure. Direct
  boolean expectations for 192 combinations and explicit multi-blocker tests
  check that D/H remain independent of I/C and K blocks only its dependents.
- Serialization risk: a generic prerequisite reason or invented A pass may look
  plausible. The validator rebuilds the fixed graph and compares all blockers and
  actual reason contents/order, with rejected mutation probes.
- Self-review repair: freeze the exported stage ordering at runtime (TypeScript
  readonly alone is insufficient); reject generic prerequisite_failed as an
  evaluated root cause so it cannot propagate as if it were a real reason.
- Packaging verification initially found the existing exact file inventory
  rejecting the new development files. Add only six named paths; keep historical
  manifests unchanged and strengthen the tarball check to exclude development.

No production source is changed. The inspected diff is confined to these two
components/tests, two development documents, explicit repository inventory,
tarball exclusion assertion and a component CI step. Existing package version,
dependencies, schemas, registry, CLI, numerical source and SOURCE-PIN stay fixed.

## Independent implementation review request

Resolve and record the delivery PR's exact head, parent, tree and file list. Read
Verifier AGENTS.md and its required documents, the linked D0 design/confirmation,
both components and their tests. Disclose any authorship/repair involvement and
source/model/environment limits. Scope is this component checkpoint only.

Try to find a raw-byte projection that violates the specified deletion span,
strict eligibility, limit ordering, UTF-8 offsets or copy isolation. Try to pass an
impossible graph, suppress an independent result, omit a blocking reason or change
the deterministic reason union. Check that development code stays outside the
actual npm tarball and no helper is mistaken for a completed verification call.
Assess whether the coverage table overclaims any full-call or host evidence.

Return GO_FOR_D1_CONTINUATION, REPAIR_REQUIRED or BLOCKED, with exact locations,
minimal repair and closure check. Preserve a fixed-head record and actual
commands/environment; mark unavailable executions NOT_RUN. No new scientific
claim or broad numerical/source review is requested. This is not D1 completion,
integration/adoption, RFC-window or publication GO.

## Next implementation unit

After the component review, connect actual local Record schema/D0/Holm-domain
validation, independently supplied expected context and declared-digest comparison
to this graph. Preserve the reviewed Python numeric files before any relocation;
use exact byte/delta evidence to assess reuse rather than rewriting their method.
Define the versioned unissued report/refusal shape, scoped results and reference
admission together with independent expected outputs. Never treat these helpers'
synthetic input objects or trusted evaluation flags as a working verifier.

Then complete the shared budget/supervisor/public-output path, all 44 expanded
cases and real-host evidence. Reconcile PR #330 against current Protocol main
without rewriting its historical pins. Only later proceed to D2 dispatcher and
conformance integration and D3 authoritative overlay. Preserve the full R3
programme, existing source exclusions and conditional R2 interface dependencies.

The delivery PR records actual local validation and CI status against its head.
This author handoff is not independent reviewer clearance. No merge, package
publication, Protocol SOURCE-PIN intake or supported dispatch activation occurs.
